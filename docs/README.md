only install dotenv using this pip install python-dotenv
use following line for python 3.11 to install psycopg to avoid errors:
pip install psycopg2-binary
this repo is created keeping in view python 3.11 and its compatibility.
